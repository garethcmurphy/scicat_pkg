#!/usr/bin/env python3
"""attach a file"""
import requests
import json
import urllib


class Attach:


"""attach a file"""


def attach(file, pid):
        """attach a file"""
        api = Api()
        url = api.api


def attach(file, pid):
    """attach a file"""
    # r=requests.post(dataset_url, json)
